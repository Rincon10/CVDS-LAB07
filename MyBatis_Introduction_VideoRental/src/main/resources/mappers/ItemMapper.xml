<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="edu.ecitem.cvds.sampleprj.dao.mybatis.mappers.ItemMapper">
    <insert id="insertarItem" parameterType="map">
        INSERT INTO VI_ITEMS (id,
                nombre,
                descripcion,
                fechalanzamiento ,
                tarifaxdia ,
                formatorenta ,
                genero)
                VALUES (#{item.id},
                #{item.nombre},
                #{item.descripcion},
                #{item.fechalanzamiento},
                #{item.tarifaxdia},
                #{item.formatorenta},
                #{item.genero})
    </insert>
    <select id="consultarItems" parameterType="map" resultMap="ItemResult">
        SELECT *
        FROM VI_ITEMS
    </select>
    <select id="consultarItem" parameterType="map" resultMap="ItemResult">
        SELECT *
        FROM VI_ITEMS
        WHERE id = #{item.id}
    </select>
    <resultMap id="ItemResult"  type="Item">
        <id property="id" column="id"/>
        <result property="nombre" column="nombre"/>
        <result property="descripcion" column="descripcion"/>
        <result property="fechaLanzamiento" column="fechaLanzamiento"/>
        <result property="tarifaxDia" column="tarifaxDia"/>
        <result property="formatoRenta" column="formatoRenta"/>
        <result property="genero" column="genero"/>
        <association property="tipo" javaType="TipoItem" resultMap="edu.ecitem.cvds.sampleprj.dao.mybatis.mappers.TipoItemMapper.TipoItemResult" columnPrefix="ti_"></association>
    </resultMap>
</mapper>